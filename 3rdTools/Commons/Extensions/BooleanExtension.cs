namespace Extensions;

public static class BooleanExtension
{
    public static bool Not(this bool source)
    {
        return !source;
    }
    public static bool Invert(this bool source)
    {
        return !source;
    }
}
